# Express_PM2_Server

horizontal scaling, load balancing with the cluster module that uses the round-robin approach
zero down-time restarts

then PM2 that is build upon the functionality of the cluster module.